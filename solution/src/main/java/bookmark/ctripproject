
问一问：
基于 Javacc 的规则引擎，基于双向 RNN 的算法模型
基于 groovy 的脚本，支持动态加载类，实时渲染答案模板
（本地脚本load 进 mysql，通过 mysql 构建缓存，后台修改数据库，然后加载进缓存，用 groovy 类加载器重新加载类）

用户问题-分词-规则匹配/模型匹配-命中大类/小类/属性，到 redis 拿结构化数据，用 groovy 渲染答案模板
后台：表达式配置（用做分词词库）
    脚本配置
    问题配置-规则命中属性
    离线表字段与大类的关系配置
数据：离线数据根据聚合上述配置聚合，之后导入 redis

特征挖掘助手架构与流程
从 UGC分词，根据 tfidf、tgi 选取关键词，导入 ck 表
人工标注关键词，运营数据落入 db
db 同步 到zeus离线平台，定位到具体的句子中，根据句法分析分析修饰词与核心词，组成短语，导入 ck
人工标注短语，运营数据落入 db
短语即可作为标签，同时 根据短语定位到子句，可以用来挖掘子句

挖掘的子句数据（线上展示和备选子句）同步至 db，可以在后台或者商家端运营，运营数据回写 db，人工操作会写到日志表，记录操作选项，操作前后的子句
db 第二天同步至 zeus 离线平台，计算点评降权等逻辑、子句质量等逻辑，备选子句同步 db，
同时为每一家酒店选取算法推荐子句（上线展示用），发送消息给 job，用于更新线上展示的子句（人工>算法推荐），同时写入日志表，记录算法操作

标签树
上述标签建立树结构存入 db，后台展示树节点，同时支持新建树节点关联到已有树结构上
对于新发掘的标签，需要关联到已有的树上，结合算法提供 2 种方式：融合：融进意思相近的树节点上、上下位：提供上级阶段用于运营操作
标签树标签与真实业务标签关系：1对多关系映射
酒店 UGC标签是否参与挖掘（参与挖掘可以与酒店建立 ugc 关系）
应用场景白名单
人工置顶标签
每天全量酒店计算标签树关系：通过查 nebula 获取业务标签，查 ck 获取 ugc 标签（循环依赖）关系分（算法计算），
    从而建立酒店和标签树的关系，（此关系没有落地的表，只有 redis，为给算法写入 ck）算法拿到关系后做排序导入 ck第二天分区
计算关系的同时，把计算后关系和分数数据存入 redis：酒店级别的关系数据，树级别的结构信息
当有业务标签变更时，job 消费重新建立关系，此时没有算法推荐分
新增酒店，job 没有感知到，不会计算关系，算法拿不到关系不会计算分数导入ugc关系表
计算关系和计算分数耦合，分数依赖关系
原始数据变更同时落 nebula 和触发关系计算，但是关系计算由依赖 nebula 查询

 Clickhouse：
 列式数据库，适合 OLAP 不适合 OLTP
 mergeTree 引擎不适合更新，不够实时
 结合 mysql，海量价值较低的待标注全量数据存入 ck，
 人工运维的高价值数据落 mysql，同时把状态写入每日新的 ck 表，当天的状态通过 小表的max（time）获取，和大表 join 取得状态
 第二天把 mysql 的数据同步到大表里的状态字段
 为什么不 max 大表的字段？ 有一些 vector 字段，读取时耗时严重，且回写时不会带入大表之前的字段，如果要写大表，就要把原大表字段全取出来



Nebula 图数据库：
架构 ：数据同步 job 监听变更消息，写入 nebula
      数据读取服务，根据请求类型，拼 ql 查询 nebula，返回数据缓存至 redis
      运营后台，支持配置 nebula schema，外部数据格式与 nebula schema 的匹配关系
遇到的问题：
1. graph 初始化链接不够用创建链接导致服务挂掉：链接是接流量才创建才做缓存，短时间大流量不够用  改进：点火时创建
    nebula bug：同时 500 条语句会报错
2. meta 服务被 wal 文件爆掉，原因 meta 服务用于管理链接 session，graph 创建的 session 会定期同步到 meta 做持久化，目的是 session 的复用；
观察到即便 graph 未接入流量，定期也有大量的 wal 文件写入，session 的生命周期：定期把 session 的信息再拉取/同步给 meta，目的是更新 session 的
使用时间， 方便 meta把过期的 session 关掉
优化：拉长同步周期，设置永不过期，主动根据 session的 exceptioin 来手动关闭
3. storage 服务：在流量大到一定地步后会崩溃，原因是锁竞争，一开始怀疑热点数据问题，重新分片，分更多片，把某一类热点数据打散；
但是没有解决根本问题，如果还是有热点数据，最终还是会请求到某个分片上；
为了减少锁竞争，通过 perf 发现引擎底层会有一层 cache，即把加解压缩后的数据缓存，使用的算法是 LRU，在涉及缓存淘汰时会加锁；
考虑到存储空间可以增加，我们关闭了压缩功能，关闭了缓存功能
DR 部署
raft 协议选举，要保证奇数
方案 一、同一集群2地3中心部署，集群内数据同步通过 nebula 内部 raft 协议保证数据同步
某一机房 down 掉时，raft 在剩下可用机房选举出 leader
优点：某一机房 down 后数据同步不需要使用方补偿，部署方便，缺点：网络通信跨机房，不能就近访问，效率较差
方案二、两个 IDC同时部署两套 nebula 集群，每个集群独立运作，客户端通过 ip 来切换流量
可以做到就近访问，只访问本 IDC的 nebula，提高网络响应时间
但是某一集群 down 后恢复后，数据同步需要使用方补偿，平时数据需要双写


携程 databus 平台搭建
1. 基于携程 QMQ 消息（第三方业务消息和数据库变更 Otter（Canel）消息）和 Oceanbase构建统一数据收集平台：
    基于消息内容，根据类型做语种、数据类型的扩散，发送统一数据格式数据消息，对统一格式数据消息消费，落地 OB；原因 可以通过中间消息批量补数
    把不同类型数据格式的数据通过统一 sdk 转换数据格式，处理多语言后实时落地至 OceanBase
    sdk：codeEnum 定义 property code和 type code，msgConverter 转换格式：不同类型的msg根据 type 分别封装 value 逻辑
2. 基于构建的 Oceanbase 统一格式数据平台，分别构造 redis 对客数据、es 外挂索引、nebula 关系计算数据、对外变更消息
3. Nebula计算关系后，抛出计算消息，由databus 服务消费落地关系数据至 OB，复用 2 逻辑即时构造对外数据（根据 tag 选取，保证不循环依赖）
多条消息消费的先后问题，eg：多条消息的字段共同组成 ob 中一条数据，多条消息不同时间消费到，读 ob 旧数据拼新数据，存在脏读问题，加锁影响性能
1.通过实时调用接口补全字段（接口要实时更新）2.落 ob 时根据时间戳做乐观锁更新（获取到后面的消息，但是调接口处理时间快，落入 ob，前面消息更新 ob 时就抛弃）
意义：
原始数据变更只影响ob，关系计算由 ob 的变更触发，不会同时触发，不存在时间差
关系计算和分数计算解耦，不存在相互依赖
UGC标签和标签树标签分成两种类型，概念不耦合
收口底层各式各样类型数据，转换为统一格式，对外保证各个页面数据一致性（IM\详情页\订后页）
    各类型的数据集中收口，利于关系计算，计算后的关系数据可以用于各种产品：搜索、筛选召回；场景、玩法圈选酒店