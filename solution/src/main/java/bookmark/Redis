单线程 多路复用 nio
持久化 rdb aof cow
主从同步
事务
发布订阅
数据类型
    string(计数器) list set sortedset(排行榜) hash
    stream bitmap
应用场景
string 计数器：incr 自增 1 原子操作
              getset 取当前值且重置某值
       分布式锁：setnx set if not exist 返回 true，代表获取锁，设置过期时间
sortedset 排行榜：zincrby sortedset对分数原子增加，排行榜实时增加分数
                 zunionstore 合并榜单生成周榜
redisObject
    为什么涉及 redisObject 对象？
    1.不同数据类型，可以执行的指令不同，需要让每个健都带有类型信息，程序可以检查类型来选择合适处理方式
    2.除了要检查键类型，还要根据数据类型的不同编码进行多态处理 eg：集合元素可以是字典和整数集合不同数据结构
    redisObject功能：
    1.基于 redisObject 对象的类型检查
    2.基于 redisObject 对象的显式多态函数
    3.对 redisObject 进行分配、共享和销毁的机制
    redisObject 结构：type 类型、encoding 编码方式、lru、refcount引用计数、ptr 指针指向底层数据结构
    1.对于给定的 key，检查字典是否有对应的 redisObject
    2.对于给定的 key，检查它的 redisObject 中的 type 与执行命令的操作类型是否一致
    3.根据 encoding 属性所指的编码，选择合适的操作函数来处理底层数据结构
    4.返回数据结构

底层数据结构
string->简单动态字符串 sds 头部保存字符串长度 len，空间预分配 执行过 APPEND命令的字符串会带有额外的预分配空间
hash(key少 value 小时)/sortedSet->压缩列表 ziplist 提高存储效率的双向链表，可以存储整数和字符串
用 encoding 来细化存储大小，这样每个元素都不定长，需要 prevLen来使指针移动一个元素的长度
list-> 快表 quicklist 以 ziplist 为节点的双端链表结构
hash/set（v 是 null）->字典 dict 数组 table
set（全部整数时）->整数表 intset 只包含整数元素的集合
sortedSet->跳表 skiplist有序集合(zset其实是 hash+skiplist)

持久化
RDB 手动触发与自动触发 （1.配置定时 2。主从复制）
    bgsave 主进程->判断是否有子进程->fork子进程->执行其他命令
                    ->有直接返回       ->写入临时 rdb 文件->替换旧 rdb 文件->通知主进程
    如何保证子进程写入磁盘时，主进程的修改？
    此时主进程的修改 copy on write 新数据副本会暂存在新内存区域，然后子进程把该内存区域内的数据写入 rdb 文件
    频繁快照的开销：1.磁盘压力 2.fork 子进程会阻塞主进程
    优点：
        1.快照内存按 lzf 压缩，内存占用小，适合备份、全量复制
        2.恢复速度快
    缺点：
        1.实时性不够 2.fork 子进程开销大 3.二进制文件可读性差 不适合实时持久化
AOF 写后日志 区别于写前日志（wal）（mysql通过wal 与二阶段提交实现数据和逻辑的一致性)
    为什么写后日志？
    1.避免额外检查开销，写入的一定是正确的命令
    2.不阻塞当前写操作
    风险 ：
    1.命令执行完但是日志写入前宕机，丢失数据
    2.主线程写磁盘压力大，写盘慢
    实现：
    1.命令追加 执行完写命令后，追加一条日志到 aof_buf缓冲区
    2.文件写入和同步 aof_buf缓冲区数据写入 aof 文件
    三种策略（appendfsync参数 -linux 的page cache）:always（同步写回）、everysec(每秒)、no（由操作系统决定）
    重写：aof 文件一直追加，体积越来越大，重写机制用于瘦身
    主进程 fork bgwriteaof 子进程，把主进程内存（虚拟页表）拷贝一份给子进程，子进程把数据写成操作，记入重写日志
    重写时机 ：1.auto-aof-rewrite-min-size重写文件的大小
             2.auto-aof-rewrite-percentage当前 aof 和上次的比值
    新数据 如何保证一致？
    "一个拷贝，两处日志"
    1.主进程会把命令记录到两个 aof 缓冲区，如果配置 always，主进程立即写入主进程旧 aof 文件中
    2.进程通知主进程完成重写后，主进程把重写缓冲内容写入新文件
    3.替换文件
    为什么 aof 重写不复用原 aof 日志？
    1.父子进程操作同一个文件产生竞争
    2.如果重写失败，污染源文件
RDB+AOF：内存快照以一定的频率执行，在两次快照之间，使用 aof 日志记录这期间的所有命令操作
    避免频繁 fork 子线程、避免 aof 文件过大
宕机恢复：aof 保存数据更完整，最多损失 1s 数据
    1.开启 aof，优先加载 aof 文件
    2.aof 加载成功，redis 恢复成功
    3.aof 不存在，rdb 存在，加载 rdb
    4.rdb 加载成功或者没有 rdb，redis 启动成功

发布订阅模式：
    1.基于 channel:publish channelname msg/ subscribe channelname
        订阅后不能执行非订阅的其他操作
        用字典实现 ：key 是 channel name，value 是客户端链表
    2.基于 pattern：publish pattern msg / psubscribe pattern
        基于对象链表，存储 client 和该 client 订阅的 pattern，通过遍历链表来发送消息
事务：
    1.执行步骤：开启 MULTI-入队-EXEC执行
    MULTI前启用 WATCH加 CAS，如果 key 的值与 WATCH时的值不一样 事务回滚/否则执行，最后取消监控
    2.为什么 redis 不支持回滚？
    redis 命令只会因语法错误失败，入队检查就报错
    不支持事务可以保持简单快速
    3.ACID : 原子性：所有命令要么全部执行，要么全不执行，而不是全部执行成功
            一致性：命令失败执行事务回滚，保证数据恢复到没执行前的样子
            隔离性：单进程单线程保证隔离性
            持久性：不保证，因为 RDB和 AOF 都是异步，出于性能考虑

主从复制：
    1.作用：数据冗余、故障恢复、负载均衡、高可用基石
    2.全量复制过程：
        第一阶段：主从库建立链接、协商同步过程
            从库发送 psync 命令，包含主库的 runId（第一次是？）和 offset（-1）
            主库第一次发送 FULLRESYNC(全量复制)命令，带上主库 ruleId 和 offset
        第二阶段：主库发送 rdb 文件，从库本地加载：同时，主库新的写操作存到 replication buffer
        第三阶段：主库把 replication buffer 里的操作 发给从库，从库执行操作
        why rdb？文件小，恢复快
    3.增量复制过程：
        从库断线重连后发送 psync 包含 offset，主库从 offset 后发送，主库发送 replication buffer 的操作数据
        repl_backlog_buffer环形缓冲区：从库断开后找到主从差异的缓冲区，写满后执行全量复制
        从库自身有 slave_repl_offset,通过 psync 命令把参数发送给主库，主库处理不同从库的 offset
        如果主库 repl_backlog_buffer的 slave_repl_offset被覆盖，就全量同步
    4.从从复制：
        主从复制需要 fork 子进程生成 rdb 文件，从库太多，需要一直 fork
        "主-从-从"模式将主库生成 RDB和传输RDB的压力，以级联的方式分散到从库上

哨兵机制
    1.功能：
    监控：不断检查主、从节点是否正常
    自动故障转移：主节点故障时，选择一个从（配置优先级最高或偏移量最大）作为主，其他从改为复制新的主
    配置提供者：客户端初始化时，通过哨兵获取 redis 服务主节点地址
    通知：把故障转移的结果发给客户端
    2.组建原理：通过发送/订阅模式：哨兵 1 通过主库发布消息，哨兵 2/3订阅消息
    3.主库下线：单个哨兵主观下线后，给哨兵集群发消息投票，大于配置 quorum 时客观下线
        主观下线：单个哨兵监控探测判断下线
        客观下线：哨兵集群判断决定下线
    4.哨兵集群的选举：主库下线后，需要有主哨兵操作主从切换
        哨兵分布式集群采用 raft 协议选举：投票树大于 num/2+1且大于 quorum（否则不能客观下线），否则继续选举

分片技术：
    1.数据分片：按 key hash 到 16384个 slot 上，cluster 不同节点负责一部分 slot
    2.故障恢复：构建 master 集群，每个 cluster 由一个 master 和多个 slave 构成，master 故障 slave自动 failover
    3.请求重定向：
        MOVE：返回错误码和正确的节点
        ASK：key 正在 migrating，返回 importing 该 key 的节点（临时状态

缓存穿透：缓存和 db 中都没有数据：接口校验、key-null、布隆过滤器
缓存击穿：缓存无 db 有：设置热点数据不过期、接口限流熔断降级、互斥锁
缓存雪崩：缓存数据大批量过期：过期时间均匀设置、热点数据分布式存储
缓存污染：永不被房屋的数据没有设置过期时间
缓存容量一般设置东数据量的 30%-40%
缓存淘汰策略：不淘汰、对设置了 ttl 的数据进行淘汰、对全部数据进行淘汰

缓存更新的策略：
    Cache Aside Pattern读：先读缓存，没有了读库，放入缓存
                        更新：先写库，再删缓存（不写缓存是担心并发写导致脏数据）
    问题：缓存无数据，线程 1 读库，写入缓存，库数据更新，那么此时缓存是旧数据
    缓存双删：更新库->删缓存（若失败可能导致不一致）->异步队列再删缓存
        进阶版：业务代码只更新库，job 订阅 binlog 来更新缓存

布隆过滤器：位数组+多个 hash 函数
    存入时，某个数据经过多个 hash 函数存入数据对应的索引
    查找时，某个数据经过多个 hash 函数得到 idx，看数组中是否全都有值
    过滤器判断有，不一定有
    过滤器判断无，一定无
    某个元素存在，过滤器一定判存在
    某个元素不存在，过滤器可能判存在
    缺点：不能删除，因为位数组只有 1
    变种：布谷鸟过滤器，每一位存计数，删除某元素时-1

redis 问题排查与调优：
    1.测试基准性能返回实例 60s 内的最大响应延迟：测试正常实例的基准性能 vs 变慢实例的基准性能
    2.排查慢日志 slowlog，复杂操作（大于 On）或者 N 太大
    3.大 key：value 太大或者 size 太大
    4.集中过期：主动过期的定时任务，不计入慢日志中
    5.达到内存上线，触发淘汰机制
    6.fork 子进程阻塞：控制实例内存大小、低峰期同步、降低全量同步频率
    7.内存大页
    8.开启 AOF：后台子线程刷盘操作与 rewrite 重合
    9。绑定 CPU